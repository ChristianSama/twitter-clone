<main class="d-flex flex-column">
  <div class="title">
    <h1>Home</h1>
  </div> 
  <!-- <p id="notice"><%= notice %></p> --->
  <form action="#" class="new-tweet">
  <%= form_with model: @tweet do |form| %>

  <% end %>
    <div class="new-tweet d-flex">
      <%= image_tag "user-icon.png", size: "75x75"%>
      <textarea name="tweet_content" placeholder="What's happening?" class="tweet-content"></textarea>
    </div>
    <div class="tweet-option-bar d-flex justify-content-between">
      <div class="icon-bar">
        <i class="">icon</i>
        <i class="">icon</i>
        <i class="">icon</i>
        <i class="">icon</i>
        <i class="">icon</i>
      </div>
      <button>Tweet</button> 
    </div>
  </form>
<!--
  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @tweets.each do |tweet| %>
        <tr>
          <td><%= tweet.body %></td>
          <td><%= link_to 'Show', tweet %></td>
          <td><%= link_to 'Edit', edit_tweet_path(tweet) %></td>
          <td><%= link_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Tweet', new_tweet_path %>
--->
</main>
<div class="recommendations w-25">
  hola
</div>